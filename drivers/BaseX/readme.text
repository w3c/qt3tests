A test driver written in XQuery is available from the following location:

 https://github.com/LeoWoerteler/fots-basex

It depends on some extensions in the BaseX product, but should be usable as a basis
for writing test drivers for other implementations.